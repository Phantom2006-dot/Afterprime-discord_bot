--only-binary=all

# Core bot & web server
discord.py==2.3.2  # Downgraded - 2.4.0 might have issues
fastapi==0.104.1  # Compatible version
uvicorn[standard]==0.24.0  # Stable version

# Database
sqlalchemy==2.0.23  # Stable version
psycopg2-binary==2.9.9  # Slightly older but stable

# Networking & Auth
requests==2.31.0
requests-oauthlib==1.3.1
aiohttp==3.8.6

# Environment & Utilities
python-dotenv==1.0.0
cryptography==41.0.8  # Pre-compiled wheel available
pydantic==1.10.12  # Use pydantic v1 instead of pydantic-core
python-multipart==0.0.6
